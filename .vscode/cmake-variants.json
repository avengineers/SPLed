{
  "variant": {
    "choices": {
      "Base/Dev": {
        "long": "select to build variant 'Base/Dev'",
        "settings": {
          "VARIANT": "Base/Dev"
        },
        "short": "Base/Dev"
      },
      "Disco": {
        "long": "select to build variant 'Disco'",
        "settings": {
          "VARIANT": "Disco"
        },
        "short": "Disco"
      },
      "Sleep": {
        "long": "select to build variant 'Sleep'",
        "settings": {
          "VARIANT": "Sleep"
        },
        "short": "Sleep"
      }
    },
    "default": "Disco"
  },
  "buildType": {
    "default": "Debug",
    "choices": {
      "Debug": {
        "short": "Debug",
        "long": "Disable optimizations and include debug information",
        "buildType": "Debug"
      },
      "Release": {
        "short": "Release",
        "long": "Optimize for speed and exclude debug information",
        "buildType": "Release"
      }
    }
  }
}
